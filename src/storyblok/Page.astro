---
// Generic Storyblok Page component — currently a passthrough
// Storyblok content is used for editable fields (phone, email, hero text)
// All other content is hardcoded in the page components
const { blok } = Astro.props;
---
<slot />
